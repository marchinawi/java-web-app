pipeline {
    agent any
    stages {
        stage('scan'){
            steps{
                withSonarQubeEnv(installationName: 'trucks Maven Webapp'){
                    sh './mvnw clean sonar:sonar'
                }
            }
        }
    }
}
