pipeline {
    agent any
    stages {
        stage('scan'){
            steps{
                withSonarQubeEnv(installationName: 'SonarQube-server'){
                    sh './mvn clean sonar:sonar'
                }
            }
        }
    }
}
