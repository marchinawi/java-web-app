pipeline {
    agent any
    stages {
        stage('scan'){
            steps{
                withSonarQubeEnv(installationName: 'SonarQube-server'){
                    sh './mvnw clean sonar:sonar'
                }
            }
        }
    }
}
